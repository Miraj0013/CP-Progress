/*
  IN THE NAME OF ALLAH
  Author:S.M. Akhlakur Rahman Meraj
  linkdin:https://www.linkedin.com/in/miraj13/
*/
#include<bits/stdc++.h>
#define yes cout<<"YES"<<'\n'
#define no cout<<"NO"<<'\n'
#define pb push_back
#define ll long long
#define fast() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define lcm(x, y) ((1LL * (x) * (y)) / __gcd((x), (y)))
 
using namespace std;
const int N=1000000009;
 
 
int main(){
  fast();
  int n;
  cin>>n;
  int arr[n],left[n];
  for (int i = 0; i < n; ++i)
  {
    cin>>arr[i]>>left[i];
  }
  sort(arr,arr+n);
  sort(left,left+n);
  int ans=0;
  for (int i = 0; i < n; ++i)
  {
    int ind=upper_bound(left,left+n,arr[i])-left;//lefft people
    ans=max(ans,(i-ind+1));
  }
  cout<<ans;
}
 
