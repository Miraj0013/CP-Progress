/*
  IN THE NAME OF ALLAH
  Author:S.M. Akhlakur Rahman Meraj
  linkdin:https://www.linkedin.com/in/miraj13/
*/
#include<bits/stdc++.h>
#define yes cout<<"YES"<<'\n'
#define no cout<<"NO"<<'\n'
#define pb push_back
#define ll long long
#define fast() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define lcm(x, y) ((1LL * (x) * (y)) / __gcd((x), (y)))
 
using namespace std;
const int N=100000;
 
 
int main(){
  fast();
  int n,m;
  cin>>n;
  vector<pair<int,int>> arr(n+1);
  arr[0].first=0;
  arr[0].second=-1;
  for (int i = 1; i <=n; ++i)
  {
    cin>>arr[i].first;
    arr[i].second=i;
  }
  sort(arr.begin(),arr.end());
  int cnt=1;
  for (int i = 1; i <= n; ++i)
  {
    if(arr[i].second<arr[i-1].second)cnt++;
  }
  cout<<cnt;
  
}
